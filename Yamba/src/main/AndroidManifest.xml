<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.twitter.university.android.yamba"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk
            android:minSdkVersion="18"
            android:targetSdkVersion="18"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
            android:name=".YambaApplication"
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        <activity
                android:name="com.twitter.university.android.yamba.TimelineActivity"
                android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.twitter.university.android.yamba.TweetActivity" />
        <activity android:name="com.twitter.university.android.yamba.TimelineDetailActivity" />
        <activity android:name=".PrefsActivity" />

        <service android:name="com.twitter.university.android.yamba.service.YambaService" />

        <provider
                android:exported="false"
                android:name="com.twitter.university.android.yamba.data.YambaProvider"
                android:authorities="com.twitter.university.android.yamba.timeline" />

        <receiver android:name="com.twitter.university.android.yamba.service.BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_RECEIVED"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>
